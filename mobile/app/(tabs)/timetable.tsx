import PlaceholderScreen from '@/components/PlaceholderScreen';

export default function TimetableScreen() {
  return <PlaceholderScreen title="Timetable" note="Weekly schedule from /api/mobile/timetable will show up here." />;
}
